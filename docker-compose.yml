# docker-compose.yml - Backend only (Frontend on Netlify)

services:
  # 1. Nginx Web Server
  nginx:
    image: nginx:alpine
    container_name: shop-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./services/be-3d:/var/www
      - ./docker/nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - app
    networks:
      - app-network
    restart: unless-stopped

  # 2. Application Backend (Laravel PHP-FPM)
  app:
    build:
      context: ./services/be-3d
      dockerfile: Dockerfile
    container_name: shop-app
    volumes:
      - ./services/be-3d:/var/www
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    env_file:        
      - .env
    environment:
      DB_HOST: mysql
      REDIS_HOST: redis
    networks:
      - app-network
    restart: unless-stopped

  # 3. Database MySQL
  mysql:
    image: mysql:8.0
    container_name: shop-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    expose:
      - "3306"
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - app-network
    restart: unless-stopped

  # 4. Redis Cache
  redis:
    image: redis:alpine
    container_name: shop-redis
    expose:
      - "6379"
    volumes:
      - redis-data:/data
    networks:
      - app-network
    restart: unless-stopped

  # 5. AI Chatbot Service (Python FastAPI)
  chatbot:
    build:
      context: ./services/chatbot-ai
    container_name: shop-chatbot
    ports:
      - "8001:8001"
    volumes:
      - ./services/chatbot-ai:/app
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge

volumes:
  db-data:
    driver: local
  redis-data:
    driver: local